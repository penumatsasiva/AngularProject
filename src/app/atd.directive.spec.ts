import { AtdDirective } from './atd.directive';

describe('AtdDirective', () => {
  it('should create an instance', () => {
    const directive = new AtdDirective();
    expect(directive).toBeTruthy();
  });
});
