import { DynamiClassDirective } from './dynami-class.directive';

describe('DynamiClassDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamiClassDirective();
    expect(directive).toBeTruthy();
  });
});
