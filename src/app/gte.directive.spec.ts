import { GteDirective } from './gte.directive';

describe('GteDirective', () => {
  it('should create an instance', () => {
    const directive = new GteDirective();
    expect(directive).toBeTruthy();
  });
});
