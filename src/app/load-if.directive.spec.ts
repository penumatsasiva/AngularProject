import { LoadIfDirective } from './load-if.directive';

describe('LoadIfDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadIfDirective();
    expect(directive).toBeTruthy();
  });
});
